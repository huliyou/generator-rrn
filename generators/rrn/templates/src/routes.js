/*
 * @flow
 */
import React from 'react';

import { Route, IndexRoute } from 'react-router';

const routes = (
  <Route path={RoutingURL.PrefixURL()} component={<div>hello</div>} >
  </Route>
);

export default routes;
